# Style & Trim Frontend Spec

## 內文規則（不寫進介面）
- 顧客前台只放理髮預約相關資訊。
- 不寫技術名詞、不提系統內部流程。
- 不放後台、資料流、角色權限、系統架構。
- 文字短句化。
- 圖片優先。
- 卡片要有主次，不能每個區塊都長一樣。
- 不出現工程實作字眼。
- 不用系統感、管理感的語氣。
- 字體禁用 `Inter`、`Roboto`、`Arial`、`system-ui` 與系統預設字體堆疊。
- 標題用有品牌感的襯線字，內文用清楚的中文字體。
- 視覺以乳白、暖米、淡金為主。
- 漸層只留給少數強調區。

## 內文

### 首頁

| 順序 | 區塊 | 內文規則（不能寫進頁面） | 內文（能寫進去的文字） | 互動 | 備註 |
|---|---|---|---|---|---|
| 1 | 首屏 Hero | 只放主視覺、主標題、短副標、兩個按鈕；不要放多餘解釋。 | `立即預約`、`查看更多作品` | 輪播切換、按鈕連結 | 先抓住風格感 |
| 2 | 品牌簡介 | 圖片與品牌感優先，品牌名只出現一次；文案只留短句。 | `Style & Trim`、`理髮預約與風格設計` | 無 | 留白多、字要小 |
| 3 | 作品區 | 作品要先於文字；每張圖只放短標籤。 | 作品圖片、`查看更多作品`、髮型標籤 | 點擊進作品頁 | 3 到 6 張作品 |
| 4 | 設計師區 | 只寫風格、擅長方向、作品感覺、是否可指定。 | 形象照、暱稱、專長短句 | hover 顯示專長、可切換下一組 | 預設 3 位 |
| 5 | 服務區 | 只留服務名稱、價格、時間、短說明；不要寫流程。 | 項目名稱、價格區間、極短介紹 | 無 | 可搭配圖片 |
| 6 | 門市資訊 | 只寫客人找得到店的資訊；地址、電話、營業時間要直接。 | 地址、電話、營業時間、店內氛圍 | 連結跳轉 | 簡短清楚 |
| 7 | 預約引導區 | 只放 3 到 4 個步驟卡；每一步只寫一句話。 | 預約步驟卡、`立即預約` | 按鈕導頁 | 讓人看懂流程 |
| 8 | Footer | 只放聯絡與品牌識別，不放說明文。 | 品牌圖標、名稱、電話、地點、社群連結 | 連結跳轉 | 左到右排列 |

### 預約頁

| 順序 | 區塊 | 內文規則（不能寫進頁面） | 內文（能寫進去的文字） | 互動 | 備註 |
|---|---|---|---|---|---|
| 1 | 主區 | 只保留客人要填的欄位，內容要單純。 | 服務選擇、加購選擇、日期、時間、是否指定設計師、聯絡姓名、聯絡電話 | 表單選擇 | 只保留必要資訊 |
| 2 | 預約摘要 | 摘要要短，資訊要一眼看懂。 | 服務、設計師、日期、時間、加購、總價 | 即時更新 | 摘要清楚、字短 |
| 3 | 完成狀態 | 完成訊息要簡短，強調下一步。 | 預約完成訊息、基本資訊、`前往我的預約` | 按鈕導頁 | 送出後顯示 |

### 登入頁

| 順序 | 區塊 | 內文規則（不能寫進頁面） | 內文（能寫進去的文字） | 互動 | 備註 |
|---|---|---|---|---|---|
| 1 | 會員主卡 | 入口感要像會員空間，不像管理系統；登入與註冊共用一個入口。 | `登入`、`註冊`、登入 / 註冊表單 | 切換模式、送出 | 入口感像會員空間 |
| 2 | 輔助視覺 | 只做情境感，不寫技術資訊；文案只留短句。 | 圖片或視覺區、短句 | 無 | 不放技術資訊 |

### 我的預約

| 順序 | 區塊 | 內文規則（不能寫進頁面） | 內文（能寫進去的文字） | 互動 | 備註 |
|---|---|---|---|---|---|
| 1 | 狀態統計 | 數字要清楚、標籤要短；只列主要狀態。 | 待確認、已確認、已完成 | 無 | 數字清楚 |
| 2 | 預約列表 | 只列單筆預約會看的資訊，不放多餘欄位。 | 預約項目、日期時間、設計師、狀態 | 篩選、取消 | 單筆卡片呈現 |

### 後台頁

| 順序 | 區塊 | 內文規則（不能寫進頁面） | 內文（能寫進去的文字） | 互動 | 備註 |
|---|---|---|---|---|---|
| 1 | 統計區 | 管理視角要清楚，但不要太吵；數字要先看懂。 | 今日預約、待處理、已完成、預估營收 | 無 | 管理視角 |
| 2 | 篩選區 | 篩選要簡單，避免太多控制項；只保留狀態與日期。 | 狀態、日期 | 篩選 | 保持簡單 |
| 3 | 列表區 | 操作按鈕要明確，列表要好掃描；每列只放必要欄位。 | 預約資料、操作按鈕 | 確認、完成、取消、改期 | 卡片或表格皆可 |
| 4 | 改期面板 | 只留改期必要欄位；改期資訊要一眼確認。 | 日期、時間、理髮師 | 儲存、關閉 | 對應單筆預約 |

### 空白與錯誤頁

| 順序 | 區塊 | 內文規則（不能寫進頁面） | 內文（能寫進去的文字） | 互動 | 備註 |
|---|---|---|---|---|---|
| 1 | 訊息區 | 訊息要短，不要解釋太多；只保留主要錯誤訊息。 | 找不到頁面訊息 | 無 | 內容短 |
| 2 | 返回按鈕 | 只保留單一返回動作。 | `回首頁` | 導頁 | 不放其他資訊 |
