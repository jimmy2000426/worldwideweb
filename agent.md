# Style & Trim Agent Guide

## 1. 這份文件的用途
這是一份給後續 agent 使用的工作守則，目標是讓接手的人可以快速理解這個專案，不要重複猜測。

如果你要找後端、資料庫、API 與登入架構細節，請改看 [backend-spec.md](/C:/Users/jimmy/OneDrive/桌面/worldwideweb/backend-spec.md)。
如果你要找前台顧客介面、內容語氣、版面與圖片方向，請改看 [frontend-spec.md](/C:/Users/jimmy/OneDrive/桌面/worldwideweb/frontend-spec.md)。

## 2. 專案定位
這是一個理髮預約網頁系統，品牌名稱為 `Style & Trim`。

### 目前頁面
- 目前前端已改為單一 React App，首頁、登入、預約、我的預約與後台都透過路由管理。
- `index.html` 是 React 入口頁。
- `login.html`、`admin.html` 已轉為舊入口的相容轉址頁。

### 目前路由
- `/`：首頁
- `/login`：登入 / 註冊頁
- `/booking`：預約頁
- `/appointments`：我的預約
- `/dashboard`：員工後台

### 目前前端角色
- `customer`：一般顧客
- `barber`：理髮師
- `admin`：管理員

### 目前前端狀態
- 前端已經有 React 路由、角色保護與預約流程。
- 目前登入與資料仍由本地 mock service 驅動，之後會切到 FastAPI API。
- 之後要改成正式後端時，頁面結構盡量沿用現有 React 元件，不要整頁重寫。

## 3. 下位 agent 的工作規則
- 先讀文件，再動手。
- 先理解現有 HTML、CSS、JavaScript 的結構，再決定怎麼改。
- 優先保留既有 `id`、`class`、按鈕名稱與頁面結構。
- 不要默默更換技術棧。
- 不要為了加功能而重做整個頁面，除非使用者明確要求。
- 若要新增功能，優先採取最小修改。
- 若修改涉及登入、權限、導頁或預約流程，要先確認不會破壞現有互動。
- 完成修改後，至少檢查首頁、登入、後台與預約流程是否仍能正常使用。

## 4. 頁面行為守則
- 未登入者只能看公開頁面。
- 未登入者嘗試預約時，應被導向登入頁。
- `admin.html` 只能讓 `admin` 或 `barber` 進入。
- `barber` 不應看到管理員專屬功能區塊。
- 導覽列按鈕顯示應依角色切換。
- 預約流程與後台流程都應維持清楚、可驗證的狀態切換。

## 5. 視覺與內容守則
- 文字說明以中文為主。
- 維持目前 Bootstrap 深色系與金色視覺風格。
- 若要調整 UI，優先沿用現有設計語言。
- 若需要新增元素，先確認是否能復用現有模組或 Modal。
- 前台介面目前是給想理髮、想預約的客人看，不是給工程、管理或內部操作人員看。
- 前台文案不得主打技術資訊，不要把 `React`、`FastAPI`、`JWT`、`權限模型`、`API`、`資料同步` 這類字眼直接寫在客人頁面上。
- 前台首頁與預約頁不能只靠大段文字堆內容，必須以圖文並重方式呈現，例如服務情境照、髮型作品照、理髮師風格照或空間氛圍照。
- 若要調整字體，優先使用更有品牌感、時尚感的字體組合，但仍要維持中文可讀性。
- 前端字體禁用 `Inter`、`Roboto`、`Arial`、`system-ui` 與其他系統預設模板感字體作為主要字體。
- 卡片不可大面積重複使用漸層設計，漸層只能保留在少數主視覺或重點 CTA。
- 頁面重點應放在服務內容、預約流程、可選時段、作品風格、信任感與行動按鈕，不要把後台概念露給顧客。

## 6. 與規格文件的關係
這份文件只負責工作守則與接手原則。  
[backend-spec.md](/C:/Users/jimmy/OneDrive/桌面/worldwideweb/backend-spec.md) 負責後端、API 與資料庫規格。
[frontend-spec.md](/C:/Users/jimmy/OneDrive/桌面/worldwideweb/frontend-spec.md) 負責顧客前台頁面的內容與視覺規格。

如果兩份文件內容衝突，先以使用者最新指示為準，然後同步更新兩份文件。
